[daemon_config]
# 控制命令Socket文件位置
socket_path = /tmp/keyboard.sock
# 键盘事件监听Socket文件位置
get_statu_path = /tmp/keyboard_get.sock

[keyboard_config]
# 键盘设备路径（自动检测，无需手动指定）
# 程序会自动扫描 /dev/input/event* 设备并识别键盘设备